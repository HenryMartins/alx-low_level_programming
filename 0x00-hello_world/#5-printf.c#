#include <stdio.h>

/**
 * main is the entry point
 *
 * return 0
 */

int main(void)
{
                       printf("with the proper grammar, but the out come is a peice of art, /n");
		       return (0)		       
